# NEVER commit a real token — it belongs only in GitHub Secrets
.env
*.env
!.env.example

# NEVER commit YouTube OAuth secrets (this repo is PUBLIC)
client_secret*.json
automation/client_secret.json
token*.json
*-oauth.json

# Python
__pycache__/
*.pyc
*.tmp

# OS
.DS_Store
Thumbs.db

# temp reel frames
content/reels/_frames/

# local-only YouTube runtime state
content/youtube-posted.json
content/daily-reel.log
